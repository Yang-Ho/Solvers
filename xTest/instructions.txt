in this directory:
1) Run the python shell
2) Run the following command: execfile("all_python.py")
3) Then to run any of the functions in coord. 

* Note: "is" is a python keyword and cannot be overloaded or used to define a function. As such, I've used IS.

Output from my terminal after I get into python shell:

>>> execfile("all_python.py")
>>> P.coord.inversion()
3
>>> P.coord.inversion([4,3,2,1])
6
>>> P.coord.rand(4)
[4, 3, 2, 1]
